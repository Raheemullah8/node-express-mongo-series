<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">

   <!-- Main Container -->
   <div class="main w-full min-h-screen flex justify-center items-center px-4 py-8">

    <!-- Card Section -->
    <div class="w-full max-w-lg bg-zinc-800 p-6 rounded-lg shadow-lg">
        
        <!-- Header Section -->
        <div class="text-center mb-8">
            <h1 class="text-2xl font-semibold text-white">Update Task</h1>
            <p class="text-sm text-zinc-400">Update the task details below</p>
        </div>

        <!-- Form Section -->
        <form action="/edit" method="POST">
            <!-- Pre-filled Task Title (current task name) -->
            <div class="mb-4">
                <label for="pre" class="block text-sm font-medium text-zinc-300">Current Task Title</label>
                <input type="text" id="pre" name="pre" class="block w-full px-4 py-3 mt-2 rounded-md outline-none bg-zinc-700 text-white focus:ring-2 focus:ring-blue-500" placeholder="Current task title" value="<%= filename %>" required>
            </div>

            <!-- New Task Title (to update) -->
            <div class="mb-6">
                <label for="new" class="block text-sm font-medium text-zinc-300">New Task Title</label>
                <input type="text" id="new" name="new" class="block w-full px-4 py-3 mt-2 rounded-md outline-none bg-zinc-700 text-white focus:ring-2 focus:ring-blue-500" placeholder="Enter new task title" required>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="w-full px-5 py-3 bg-yellow-500 hover:bg-yellow-400 text-white font-semibold rounded-md transition-colors transform hover:scale-105">
                Update Task
            </button>
        </form>
    </div>
   </div>

</body>
</html>
