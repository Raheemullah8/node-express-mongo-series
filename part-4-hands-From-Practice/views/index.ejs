<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form py-10 text-white">
            <form action="create" method="post">
                <input type="text" name="title" class="block px-5 py-2 w-full rounded-md outline-none bg-zinc-800" placeholder="enter your task">
                <textarea name="details" class="block px-5 py-2 bg-zinc-800 mt-2 w-full rounded-md outline-none resize-none" placeholder="Write task details here"></textarea>
                <input class="mt-2 mx-2 px-5 py-1 rounded-md text-bold bg-blue-500" type="submit" value="create">
            </form>
        </div>
        
        <div class="task p-10 flex justify-center gap-10">
            <% if(files.length > 0) { %>
                <% files.forEach((file) => { %>
                    <div class="w-60 h-30 bg-zinc-800 rounded-md">
                        <h1 class="text-bold mx-5 text-white"><%= file %></h1>
                        <a href="" class="mx-5 text-blue-300">Read More...</a>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="w-60 h-30 bg-zinc-800 rounded-md">
                    <p class="bg-zinc-700 font-bold text-white">Task Not Available</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
